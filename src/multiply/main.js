import { multiply } from 'ramda';


// `$ rollup -c` in terminal will produce `./build.js` file.
// `multiply` function is globally available in `./build.js` file
// Paste the content of `./build.js` to `Solution:` pane of http://www.codewars.com/kata/multiply/train/javascript
// Click Attempt
// Problem solved.

// Bit of overkill here but just demonstrating.


// Create next problem folder using `$ node create PROBLEM_NAME` in terminal to start working on next problem.